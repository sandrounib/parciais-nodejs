<!DOCTYPE html>
<html lang="pt-br">
<head>
  <%- include('./parciais/cabecalho.ejs') %>
</head>
<body>  
    <header>
         <%- include('./parciais/nav.ejs') %>
    </header>

    <div class="parciais-nodejs">
        <h2>Parciais nodejs</h2>
        <% if (menuResponsivo.length > 0 ){ %>
        <% menuResponsivo.forEach(menu => { %>
            <h3 class="titulo"><%= menu.titulo %></h3>
            <p class="conteudo"><%= menu.conteudo %></p>

        <% }) %>
        <% } else { %>
            <p>A lista das parciais está vazia</p>
        <% } %>
    </div>  
   <main>
       <h1>Atividade 2 parciais-nodejs</h1>
   </main>
<footer>
    <%- include('./parciais/footer') %>
</footer>    
    <script src="app.js"></script>
</body>
</html>